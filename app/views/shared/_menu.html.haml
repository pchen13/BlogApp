= link_to 'Home', root_url
- if user_signed_in?
    = link_to 'My Blog', user_path(current_user)
    = link_to 'New Post', new_user_post_path(current_user)
    // - if @post && @post.owner?(current_user)
    //     = link_to 'Delete', user_post_path(@user, @post), :method => 'delete', :confirm => 'Are you sure?'
