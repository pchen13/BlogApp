= form_for @user, :url => '/authenticate' do |f|
    .form-row
        = f.label :name
        = f.text_field :name
    .form-row
        = f.label :password
        = f.password_field :password
    .form-row
        = f.submit 'login'