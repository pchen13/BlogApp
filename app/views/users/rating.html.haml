rate #{@user.email}'s blog
= form_for [@rate], :url => rate_user_path do |f|
    .form-row
        = f.label :score, 'Enter your rating between 1-10'
        = f.number_field :score, :min => 1, :max => 10
    .form-row
        = f.submit 'Rate it'